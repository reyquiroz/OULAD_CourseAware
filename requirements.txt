# Core data science libraries
# Verified installed versions (Python 3.11.11, pyenv): pandas 3.0.3, numpy 2.4.6
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Machine learning libraries
# Verified: scikit-learn 1.9.0, xgboost 3.2.0, lightgbm 4.6.0
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0
lightgbm>=4.0.0,<5.0.0

# Additional ML utilities
# Verified: scipy 1.17.1, joblib 1.5.3
scipy>=1.10.0,<2.0.0
joblib>=1.3.0,<2.0.0

# Graph artifact serialisation (required for to_parquet in graph_pipeline.py)
# Verified: pyarrow 24.0.0
pyarrow>=14.0.0

# Graph neural network dependencies (requires Python <=3.12)
# Verified installed via PyTorch wheel index: torch 2.12.1, torch-geometric 2.8.0
# Install command (CPU):
#   pip install torch torch-geometric --index-url https://download.pytorch.org/whl/cpu
# Install command (CUDA 12.1):
#   pip install torch torch-geometric --index-url https://download.pytorch.org/whl/cu121
# NOTE: plain `pip install -r requirements.txt` will attempt PyPI for these;
#       use the index-url commands above for a working install.
torch>=2.0.0
torch-geometric>=2.3.0

# Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.25.0
jupyterlab>=4.0.0

# Optional: for notebook conversion
jupytext>=1.15.0

# Development tools (optional)
black>=23.0.0
flake8>=6.0.0
pytest>=7.4.0